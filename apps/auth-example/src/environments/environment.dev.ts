import { Environment } from "./environment";

const environment: Environment = {
    // TODO: Replace with ExampleApp URLs once CORS is configured
    apiUrl: "https://api.kontomierz.test.lncd.pl",
    authUrl: "https://auth.kontomierz.test.lncd.pl",
};

export default environment;
