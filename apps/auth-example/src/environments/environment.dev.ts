import { Environment } from "./environment";

const environment: Environment = {
    apiUrl: "https://api.exampleapp.test.lncd.pl",
    authUrl: "https://auth.exampleapp.test.lncd.pl",
};

export default environment;
