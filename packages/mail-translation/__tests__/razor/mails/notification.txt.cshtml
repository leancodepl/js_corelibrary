{{t "notification_title"}}

{{t "user_greeting", (name: "@Model.User.FullName")}}

{{t "status_update", (status: "@Model.Status", date: "@Model.UpdateDate.ToString(\"yyyy-MM-dd\")")}}

{{t "action_required", (action: "@Model.RequiredAction")}}

{{t "action_button"}}: @Model.ActionUrl

{{t "reference_number", (ref: "@Model.ReferenceNumber")}}

{{t "support_contact"}} 