import { Accept } from "react-dropzone"

export const defaultAccept: Accept = {
  "image/*": [],
}

export const defaultCrop = { x: 0, y: 0 }
export const defaultZoom = 1
export const defaultRotation = 0
