const a11y = require("./lib/a11y")
const base = require("./lib/base")
const baseReact = require("./lib/base-react")
const imports = require("./lib/imports")
const resolveFlatConfig = require("./lib/resolveFlatConfig")

module.exports = {
  imports,
  base,
  baseReact,
  a11y,
  resolveFlatConfig,
}
