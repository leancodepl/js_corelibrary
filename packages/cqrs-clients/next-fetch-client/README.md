# next-fetch-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build next-fetch-client` to build the library.

## Running unit tests

Run `nx test next-fetch-client` to execute the unit tests via [Jest](https://jestjs.io).
